import React from 'react'

const customButton = ({ type, title, customStyles, title }) => {
  return (
    <button 
    className={'px-2'}
    >
        {title}
    </button>
  )
}

export default customButton